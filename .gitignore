# Rendered outputs (regenerable)
data/generated_stimuli/

# Bundled Blender / archives (download separately; too large for git)
# Matches both a real dir and a local convenience symlink.
blender-*
*.zip

# Python
__pycache__/
**/*.pyc

# OS
**/.DS_Store
